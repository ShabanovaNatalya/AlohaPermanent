export type TMasters = {
  category: string;
  name: string;
  photo: string;
  experience: string;
  achievements: string[];
  quote: string;
};

export type TServices = {
  name: string;
  category: string;
  experience: string;
  photo: string;
  price: number;
};

export type TFaqList = {
  title: string;
  content: string;
};
