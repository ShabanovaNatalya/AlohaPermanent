export type TMasters = {
  category: string;
  name: string;
  photo: string;
  experience: string;
  achievements: string[];
  quote: string;
};

export type TServices = {
  name: string;
  photo: string;
};

export type TFaqList = {
  title: string;
  content: string;
};

export type TFeedbackList = {
  author: string;
  date: string;
  content: string;
};
